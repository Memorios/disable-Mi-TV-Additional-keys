[package]
name = "disable-mi-tv-additional-keys"
version = "0.1.0"
edition = "2021"

[dependencies]
winapi = { version = "0.3", features = ["winuser", "libloaderapi", "handleapi"] }
kernel32 = "0.5.1"
